import React, { FC } from "react";
import { Layout } from "../molecules";
import { ComingSoon } from "../organisms";

export const ToolsTemplate: FC = () =>
  <Layout direction="up">
    <ComingSoon/>
  </Layout>;
